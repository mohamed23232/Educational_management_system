<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= assignment.title %> - Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }

    h1, h3 {
      margin-bottom: 10px;
    }

    form textarea {
      width: 100%;
      height: 150px;
      margin-top: 10px;
    }

    button {
      margin-top: 10px;
      padding: 8px 16px;
      background-color: #2ecc71;
      border: none;
      color: white;
      border-radius: 4px;
      cursor: pointer;
    }

    .back-button {
      margin-bottom: 20px;
    }

    .back-button a {
      background-color: #3498db;
      padding: 6px 10px;
      color: white;
      text-decoration: none;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="back-button">
    <a href="/assignment/view">← Back to Assignments</a>
  </div>

  <h1><%= assignment.title %></h1>
  <p><strong>Description:</strong> <%= assignment.description %></p>
  <p><strong>Due Date:</strong> <%= assignment.dueDate.toDateString() %></p>

  <% if (userRole === 'student') { %>
    <hr>
    <form action="/submission/<%= assignment._id %>" method="POST" onsubmit="return confirm('Are you sure you want to submit this assignment?');">
      <label for="content"><strong>Your Submission:</strong></label><br>
      <textarea name="content" required placeholder="Type your answer here..."></textarea><br>
      <button type="submit">Submit Assignment</button>
    </form>
  <% } %>
</body>
</html>
