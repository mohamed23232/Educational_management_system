<!DOCTYPE html>
<html>

<head>
    <title>Grade Submission</title>
</head>

<body>
    <h1>Grading Submission for <%= submission.student.username %>
    </h1>

    <h3>Submission Content:</h3>
    <p>
        <%= submission.content %>
    </p>

    <form method="POST" action="/assignment/grade/<%= submission._id %>">
        <label for="grade">Grade:</label><br>
        <input type="text" id="grade" name="grade" required value="<%= submission.grade || '' %>"><br><br>

        <label for="feedback">Feedback:</label><br>
        <textarea id="feedback" name="feedback" rows="4" cols="50"
            required><%= submission.feedback || '' %></textarea><br><br>

        <button type="submit">
            <%= submission.grade !==undefined && submission.grade !==null ? 'Update Grade' : 'Submit Grade' %>
        </button>

    </form>

    <br>
    <a href="/assignment/view">Back to Submissions</a>
</body>

</html>